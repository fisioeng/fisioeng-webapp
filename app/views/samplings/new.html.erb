<% content_for :nav do %>
  <%= render "common/top_nav" %>
<% end %>

<div class="container">
  <% breadcrumb( :new_sampling, @company, @branch, @analyte ) %>

  <div class="page-header">
    <%= link_to company_branch_analyte_path(@company, @branch, @analyte), :class => 'btn btn-default' do %>
      <span class="glyphicon glyphicon-list-alt"></span>
      Back
    <% end %>
    <h1>Add Sampling</h1>
  </div>

  <%= render 'form' %>
</div>
